// ============================================================
// Token Estimation
// ============================================================

// 换算实现已上移到 `@zcode/shared`：UI 包引用不到 CLI，而两端需要共用同一套
// 字符→token 换算，各写一份必然漂移。这里保留原导出路径，既有调用点无需改动。
export { estimateTokens } from "@zcode/shared";
